# Archivos compilados
*.o
*.elf
*.hex
*.wav

# Audios de prueba (no se versionan, pero se mantiene la carpeta)
audio-samples/*
!audio-samples/.gitkeep

# Python (entorno de uv y caches)
.venv/
__pycache__/
*.pyc

# Credenciales Wi-Fi de los tests (se versiona solo secrets.h.example)
secrets.h
